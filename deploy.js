// Depreciado - Utilize o comando 'npm run deploy' (clasp push) para efetuar o deploy.
// As credenciais de deploy agora são configuradas via CLASP.
console.log("Este script foi depreciado em favor do CLASP. Utilize 'npm run deploy' (clasp push).");
process.exit(1);
